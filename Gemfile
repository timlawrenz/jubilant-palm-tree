source 'https://rubygems.org'

ruby '>= 2.7.0'

# Core dependencies for Ruby AST processing
gem 'parser', '~> 3.0'

# Standard library gems (often included but explicit for clarity)
gem 'json', '~> 2.0'

group :development do
  # Testing framework - now required for RSpec formatter
  gem 'rspec', '~> 3.0'
  # gem 'rubocop', '~> 1.0'   # Code linting
  # gem 'yard', '~> 0.9'      # Documentation generation
end

group :test do
  # Future test dependencies
  # gem 'simplecov', '~> 0.21' # Code coverage
end